# Test Environment Configuration
NODE_ENV=test

# Database Configuration (Test Database)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=ARChi0272
DB_NAME=nkabom_daycare_test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRES_IN=24h

# API Configuration
PORT=3001

# AWS S3 Configuration (Use test bucket or mocks)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_S3_BUCKET=test-bucket

# Arkesel SMS Configuration (Use test credentials)
ARKESEL_API_KEY=test-arkesel-key
ARKESEL_SENDER_ID=TestSMS

# OTP Configuration
OTP_EXPIRY_MINUTES=5
